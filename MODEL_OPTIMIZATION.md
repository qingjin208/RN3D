# 3D 模型加载优化说明

## 已实现的优化功能

### 1. **Draco 压缩支持**（运行时自动解码）
- ✅ 添加了 `DRACOLoader` 支持
- ✅ 可以自动解码 Draco 压缩的 GLB 文件
- ✅ Draco 压缩通常可以减少 70-90% 的文件大小

**如何使用：**
使用在线工具（如 [glTF Report](https://gltf.report/)）将你的 GLB 文件进行 Draco 压缩，然后替换到 `assets/models/` 文件夹即可。程序会自动识别并解码。

### 2. **提前异步预加载机制**
- ✅ 实现了"提前异步加载"策略
- ✅ 当前模型加载完成后，自动在后台预加载下一个模型
- ✅ 切换模型时几乎无延迟

**工作原理：**
```
用户查看 Model A → 等待 2 秒 → 后台开始预加载 Model B → 用户切换到 Model B 时瞬间显示
```

### 3. **内存优化**
- ✅ 纹理质量自动降低（减少显存占用）
  - 禁用 mipmap 生成
  - 降低各向异性过滤
  - 使用线性过滤
  
- ✅ Canvas 渲染优化
  - 限制像素比范围 `[1, 2]`
  - 关闭抗锯齿（对大模型性能提升明显）
  - 禁用不必要的渲染特性

- ✅ 资源释放增强
  - 切换模型时强制清理 GPU 资源
  - 清除 Three.js 缓存
  - 触发垃圾回收提示

### 4. **大模型检测与警告**
- ✅ 自动检测超过 40MB 的模型
- ✅ 显示警告信息提醒用户可能的内存问题
- ✅ 根据模型体积动态调整渲染策略

## 针对 53MB 模型闪退的建议

### 立即可做的（无需修改代码）：

1. **使用 glTF Report 压缩模型**
   - 访问 https://gltf.report/
   - 上传你的 53MB GLB 文件
   - 启用 Draco 压缩（推荐级别 8-10）
   - 压缩后可能只有 5-10MB
   - 下载并替换原文件

2. **在 Blender 中优化模型**
   - 减少面数（Decimate Modifier）
   - 压缩纹理尺寸（4K → 1K 或 512px）
   - 移除不可见的几何体
   - 合并材质球

### 代码层面的保护（已实现）：

当前代码已经包含以下保护措施：
- 懒加载：只加载当前选中的模型
- 及时释放：切换时立即清理前一个模型
- 纹理降质：自动降低大模型的纹理质量
- 渲染优化：限制像素比和抗锯齿

## 进一步优化建议

如果压缩后的模型仍然导致闪退，可以考虑：

1. **分割模型**：将大模型拆分为多个小文件，按需加载
2. **LOD 系统**：准备高、中、低三个精度版本，根据距离切换
3. **流式加载**：只加载可见部分的几何体（需要服务器支持）

## 测试方法

1. 运行应用：`npx expo start`
2. 选择一个大型模型
3. 观察控制台输出，应该能看到 "Preloaded: xxx" 的日志
4. 快速切换模型，体验预加载带来的流畅性

## 技术细节

### Draco 解码器
- 使用 Google 官方的 Draco WebAssembly 解码器
- 从 CDN 自动加载：`https://www.gstatic.com/draco/versioned/decoders/1.5.6/`
- 首次加载可能需要几秒钟下载解码器

### 预加载策略
- 延迟 2 秒启动（避免影响当前模型加载）
- 循环预加载列表中的下一个模型
- 缓存最近预加载的模型 URI

### 内存管理
- 使用 `useGLTF.clear()` 清除缓存
- 调用 `disposeObject3D()` 释放 GPU 资源
- 禁用 mipmap 可节省约 33% 的纹理内存
